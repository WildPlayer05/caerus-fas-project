# Datasource provisioning: collega automaticamente Grafana a Prometheus
# all'avvio, senza bisogno di configurarlo a mano dalla UI.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
